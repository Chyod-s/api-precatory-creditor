[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'gunicorn -w 2 -b 0.0.0.0:$PORT src.api_main.main:app'"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
